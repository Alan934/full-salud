{
  "version": 2,

  "builds": [
    {
      "src": "main.js",
      "use": "@vercel/static-build"
    }
  ],

  "routes": [
    {
      "src": "/api/(.*)",
      "methods": ["GET", "POST", "PUT", "DELETE", "PATCH", "OPTIONS"],
      "dest": "dist/main.js"
    }
  ]
}
